import { getBaseURL, getHostName } from './getBaseURL.util';
import { createUser, switchActive } from './user.util';
import registerComponents from './registerComponents.util';

export {
  getBaseURL,
  registerComponents,
  getHostName,
  createUser,
  switchActive,
};
